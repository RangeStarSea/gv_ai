<div class="content">
  {{!-- 使用自定义 widget - 方式1: 使用默认模板（无插槽，使用默认参数） --}}
  <h3>产品特性（默认参数）</h3>
  {{#> wal_rows space=(divide row.space 2)}}
    {{> custom_row items=features row=row col=col}}
  {{/wal_rows}}

  {{!-- 使用自定义 row vol - 方式3: 通过配置对象传递参数 --}}
  {{#if row}}
  <h3>产品特性（自定义间距和对齐）</h3>
  {{#> wal_rows negativeSpaceTopBottom=(divide row.space 2) gap="24px"}}
    {{#> custom_row items=features row=row col=col width='60%'}}
      {{> partials/p_txt text=this.title }}
      {{!-- 使用 h_txt 组件 --}}
      {{> partials/h_txt level=3 text=this.title }}
      {{> partials/p_txt line=2 text=this.description }}
    {{/custom_row}}
    {{#> custom_row items=features2 row=row2 col=col2 width='40%'}}
      {{> partials/p_txt text=this.title }}
      {{!-- 使用 h_txt 组件 --}}
      {{> partials/h_txt level=3 text=this.title }}
      {{> partials/p_txt line=2 text=this.description }}
    {{/custom_row}}
  {{/wal_rows}}
  {{/if}}

  {{!-- Flex Layout 示例：根据插槽数量自动左右布局 --}}
  <h3>Flex Layout 示例（自动检测插槽数量）</h3>
  {{#> flex_layout gap="24px" equal="true"}}
    <div style="background: #f0f0f0; padding: 20px; border-radius: 8px;">
      <h4>插槽 1</h4>
      <p>这是第一个插槽内容</p>
    </div>
    <div style="background: #e0e0e0; padding: 20px; border-radius: 8px;">
      <h4>插槽 2</h4>
      <p>这是第二个插槽内容</p>
    </div>
  {{/flex_layout}}

  {{!-- Flex Layout 示例：左右布局（左侧占更多） --}}
  <h3>Flex Layout 示例（左右布局）</h3>
  {{#> flex_layout  class="left-right wal" negativeSpaceTopBottom=(divide row.space 2) gap="24px"}}
    {{!-- 2/3 --}}
    {{#> custom_row items=features row=row col=col width='60%'}}
      {{> partials/p_txt text=this.title }}
      {{!-- 使用 h_txt 组件 --}}
      {{> partials/h_txt level=3 text=this.title }}
      {{> partials/p_txt line=2 text=this.description }}
    {{/custom_row}}
    {{!-- 1/3 --}}
    {{#> custom_row items=features2 row=row2 col=col2 width='40%'}}
      {{> partials/p_txt text=this.title }}
      {{!-- 使用 h_txt 组件 --}}
      {{> partials/h_txt level=3 text=this.title }}
      {{> partials/p_txt line=2 text=this.description }}
    {{/custom_row}}
  {{/flex_layout}}

  {{!-- Flex Layout 示例：3个插槽自动布局 --}}
  <h3>Flex Layout 示例（3个插槽）</h3>
  {{#> flex_layout gap="20px" equal="true"}}
    <div style="background: #f0f0f0; padding: 20px; border-radius: 8px;">
      <h4>插槽 1</h4>
    </div>
    <div style="background: #e0e0e0; padding: 20px; border-radius: 8px;">
      <h4>插槽 2</h4>
    </div>
    <div style="background: #d0d0d0; padding: 20px; border-radius: 8px;">
      <h4>插槽 3</h4>
    </div>
  {{/flex_layout}}

  {{!-- 使用自定义 widget - 方式4: 使用 Block Partials 插槽（模板继承，无需helper） --}}
  <h3>团队成员（带插槽和自定义参数）</h3>
  {{#> custom_row items=teamMembers space=24}}
  {{!-- 插槽内容：自定义按钮和标签 --}}
  <h4>{{this.title}}</h4>
  <p>{{this.description}}</p>
    {{#if this.link}}
      <a href="{{this.link}}"
        style="display: inline-block; margin-top: 10px; padding: 8px 16px; background: #4A90E2; color: white; text-decoration: none; border-radius: 4px;">查看详情</a>
    {{else}}
      <span
        style="display: inline-block; margin-top: 10px; padding: 8px 16px; background: #95a5a6; color: white; border-radius: 4px;">暂无链接</span>
    {{/if}}
    {{#if @first}}
      <span
        style="margin-left: 10px; padding: 4px 8px; background: #e74c3c; color: white; border-radius: 4px; font-size: 12px;">首个</span>
    {{/if}}
    {{#if @last}}
      <span
        style="margin-left: 10px; padding: 4px 8px; background: #7ed321; color: white; border-radius: 4px; font-size: 12px;">最后</span>
    {{/if}}
  {{/custom_row}}

  {{!-- 渲染 structuredBox 数据 --}}
  <p>========================================================</p>
  {{#if structuredBox}}
    <div class="wal">
      {{#each structuredBox}}
          {{> render_structured_item this}}
      {{/each}}
    </div>
  {{else}}
    {{!-- 调试：如果 structuredBox 不存在，显示提示 --}}
    <div style="padding: 20px; background: #f0f0f0; border: 1px solid #ccc;">
      <p>⚠️ structuredBox 数据不存在或为空</p>
      <p>请检查 config/data/index.js 中的 structuredBox 数组是否正确配置</p>
    </div>
  {{/if}}

</div>
